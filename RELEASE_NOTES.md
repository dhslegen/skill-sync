# 版本历史

发版说明的唯一来源。`scripts/publish-release.sh` 读取对应版本的章节作为发布正文,
README 的版本历史同样来自这里。

> 发版前在顶部新增一段 `## <版本号> —— <主题>`,否则脚本拒绝发布。
> 面向使用者:写改了什么、会看到什么变化,不写实现细节。

<!-- 新版本加在这一行下面 -->

## 0.6.0 —— 技能保留在原目录

**技能位置**

- 技能保留在你写它的目录,应用不再搬动文件
- 在 `~/.claude/skills/` 等 AI 工具目录里新建的技能自动出现在「我的技能」
- 取回新版本时覆盖的是原文件,路径不变
- 同一技能在多个目录内容不同时,可选择保留哪一份,其余移入废纸篓
- 每个 AI 工具一个勾选开关;技能所在目录的那个工具恒为勾选
- 目标位置被内容不同的同名技能占用时不覆盖,提示具体是哪个工具并可打开文件夹
- 移除「修复」按钮:某个工具用不了这个技能时,重新勾一下即可

**删除可撤销**

- 移除技能、被替换的旧版本、落选的版本均移入系统废纸篓
- 相应取消二次确认弹窗

**分享**

- 分享不再需要填写表单,名称与描述取自 `SKILL.md`
- 分享前按 Agent Skills 标准校验 `name` 与 `description`,不合格时禁用并说明原因
- 该校验对「分享改动」同样生效
- 同名冲突时提示改名,不提供覆盖他人技能的入口
- 移除独立的「分享」页,新建与分享并入「我的技能」

**「我的技能」**

- 按公司技能库分为三栏:安装自 / 已分享到 / 可分享到,空栏不显示
- 每行至多一个操作按钮,仅在有事要处理时出现
- 「贡献更改」统一提交审核,不直接覆盖
- 详情页新增「在哪」:本机路径、各工具启用状态、技能库状态、项目位置
- 三栏可折叠,标题显示技能数与待处理数;标题吸顶;支持搜索
- 分「通用」与「项目里」两个页签,项目内技能支持事后改选工具
- 工具勾选控件在四处统一

**技能商店**

- 状态按本机实际情况判断:获取 / 已在电脑上 / 与库里不同 / 更新
- 「更新」仅在本地未修改时出现
- 本机已有的技能不再显示为「获取」,包括用 npx skills 装的和手动放进去的
- 不再按「是否由你分享」区分按钮文案

**界面**

- 详情页的操作集中到底部一行
- 列表与页脚的「…」菜单不再被容器裁掉
- `SKILL.md` 里的表格正常渲染,超宽表格可横向滚动

**已知限制**

- 项目文件夹内的技能不参与三栏分类,不支持自动更新与分享回技能库
- 没有安装记录的技能不参与自动更新检查

## 0.5.0 · 2026-08-22 —— 项目级安装

**装进指定文件夹**

- 技能可以只装进某个文件夹,仅在那里生效
- 入口在「获取」旁的下拉菜单,可选最近用过的文件夹
- 选定文件夹后先确认再写入,确认时可就地更换文件夹
- 已装过的文件夹在点击前就标出,可覆盖重装
- 与 `npx skills` 使用相同的存放方式与记录格式,双方互相识别
- 「我的技能」新增按文件夹分组的项目分区
- 详情页新增「已装到」,列出全局与各项目位置
- 从列表移除项目不改动文件夹内容

**升级日志**

- 升级后首次打开时,商店页顶部显示本版改动,关闭后不再出现
- 跨多个版本升级时,漏看的版本收在卡片下方
- 「设置 → 版本历史」可查看全部版本

**已知限制**

- 自动更新只覆盖装在这台电脑上的技能
- 项目内改过的技能不支持分享回技能库
- 来源无法确定的技能不提供「更新」

## 0.4.0 · 2026-08-20 —— 技能广场

- 商店新增「技能广场」来源,可搜索并安装 skills.sh 收录的公开技能
- 首屏显示热门技能,无需先搜索
- 搜索按回车触发,列表滚动到底自动加载
- 装入后与公司技能库的技能用法一致,参与自动更新
- 详情与安装改为按需取用单个技能的内容,速度显著提升
- macOS 启动时的钥匙串询问由三次减为一次(需重新登录一次生效)

技能广场需要访问 skills.sh 与 github.com;访问不到时不影响公司技能库。

## 0.3.13 · 2026-08-11 —— 修复窗口拖动

- 修复两个平台上窗口都拖不动的问题,根因是缺少一项系统权限声明

## 0.3.12 · 2026-08-11 —— 更新与窗口行为

- Windows:应用更新改为后台下载,点「立即重启」后才安装
- macOS:关闭窗口后点程序坞图标可重新打开窗口
- 托盘图标改为左键打开窗口、右键弹出菜单
- 本版声称修复的窗口拖动问题实际未解决,以 0.3.13 为准

## 0.3.11 · 2026-08-07 —— 修复 Windows 登录

- 修复登录凭证超出 Windows 凭据管理器单条大小限制导致的登录失败
- 登录过程记录诊断日志(`%USERPROFILE%\.skillsync\logs\`),不含令牌与授权码
- 首次启动向导显示登录失败的原因

## 0.3.10 · 2026-08-07 —— 首个 Windows 版本

- 提供 Windows 64 位安装包,按当前用户安装,无需管理员权限
- 安装包尚未代码签名,首次运行需在 SmartScreen 提示中选择「仍要运行」
- 自动更新覆盖 macOS 与 Windows
- macOS 无功能变化

## 0.3.9 · 2026-08-07 —— 修复作者栏不显示

- 修复作者与贡献者不显示的问题,升级后自动重建索引缓存

## 0.3.8 · 2026-08-07 —— 作者与贡献者

- 技能卡片与详情页显示作者与贡献者,随索引一起下发,不额外联网
- 分享技能时自动维护归因信息

## 0.3.7 · 2026-08-07 —— 默认检查频率

- 全新安装的技能检查频率默认改为每 5 分钟,已有设置不变

## 0.3.6 · 2026-08-07 —— 新增检查档位

- 技能自动检查新增「每 5 分钟」
- 本地配置格式随之升级,装过本版后不可回装更早的版本

## 0.3.5 / 0.3.4 · 2026-08-07 —— 自更新链路验证

- 为验证 0.3.3 的自更新与重启修复而发布,无功能改动

## 0.3.3 · 2026-08-07 —— 更新检查与重启

- 应用更新检查改为每分钟一次,与技能检查频率解耦
- 修复 macOS 上重启后窗口位于其他应用之后的问题

## 0.3.2 · 2026-08-07 —— 手动更新提示

- 从设置页手动安装更新后同样显示重启提示

## 0.3.1 · 2026-08-06 —— 自更新链路验证

- 为验证 0.3.0 的自更新链路而发布,无功能改动

## 0.3.0 · 2026-08-06 —— 多来源与新建技能

- 支持添加自定义技能库来源与 GitHub
- 支持新建技能
- 应用更新改为后台装好、左下角提示重启

## 0.2.2 / 0.2.1 · 2026-08-05 —— 修复自更新

- 修复 0.1.0 与 0.2.0 收不到自动更新的问题
- 使用这两个版本需手动安装一次新包
- 0.2.0 已下架

## 0.2.0 · 2026-08-05 —— 冲突处理与标签

- 更新技能时若本地有改动,提供保留本地、用库里的覆盖、保留并分享三种处理
- 「我的技能」分区展示,商店支持标签筛选与精选清单

## 0.1.0 · 2026-08-04 —— 首个内部测试版本

- 技能商店浏览、获取、自动更新、分享到技能库
- 本版收不到自动更新,需手动安装新包

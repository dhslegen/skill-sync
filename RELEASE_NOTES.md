# 版本历史

每个版本的发版说明。**这是发版说明的唯一真相**:`scripts/publish-release.sh`
发版时会读取对应版本的章节作为内网 release 的正文,README 的版本历史也从这里来
——不手抄第二份,免得三处说法不一致。

> 发版前先在本文件顶部加一段 `## <版本号> —— 一句话主题`,否则脚本会拒绝发布。
> 写给**使用者**看:说清这一版改了什么、他们会看到什么变化,不写内部实现细节。

<!-- 新版本加在这一行下面 -->

## 0.6.0 —— 你的技能,在哪都是同一份

这一版把「技能到底在哪」这件事理顺了。以前应用只认一个固定的技能文件夹,
你在 Claude Code 自己的技能目录里写的东西,它要么看不见,要么取回时把你那份删掉、
另起一份。现在不会了:**你的技能就住在你写它的那个文件夹里,应用永远不搬动它**。

**你在 AI 工具里写的技能,直接就在「我的技能」里**

- 在 `~/.claude/skills/`(或别的 AI 工具的技能目录)里新写一个技能,打开应用就看得到,
  不用先搬到别处,也不用做任何登记。
- 点「取回」把技能库里的新版本拿下来时,**更新的就是你正在编辑的那一份文件**,
  路径不变——你可以接着在原来的编辑器窗口里改下去。
- 同一个技能在好几个工具目录下各有一份、内容还不一样时,应用会让你**选一份保留**
  (看得到各自的路径、文件数和最后修改时间),其余的移到废纸篓。

**每个 AI 工具一个勾**

- 每个技能下面是一排勾:勾上,那个工具就能用它;取消勾,它就用不到了,文件本身不动。
- 技能所在的那个工具**恒定勾着、也取消不掉**,标着「本体在这里」——文件就在它的
  目录里,谈不上关掉。
- 某个位置已经被一份**内容不一样**的同名技能占着时,应用**不会覆盖它**,而是直说是
  哪个工具,并给一个「打开文件夹」让你自己看一眼。
- 以前那个专门的「修复」按钮没有了:让某个工具能用它,本来就是这个勾自己的事,
  点一下就会顺带把断掉的接上,不必先弄明白"哪里坏了"。

**删除改成移到废纸篓**

移除技能、被替换掉的旧版本、拍板时落选的那几份,统统进系统废纸篓,能捞回来。
所以那些"确定吗?真的确定吗?"的第二道红色警示都撤掉了——**可逆比追问管用**。
反过来,原先写着"无法找回"的几句提示也都改了:它们说的那些动作,今天都能从废纸篓里捞回来。

**分享不用再填表单**

- 点「分享」直接进确认屏:名称、描述、文件夹名全部取自你写好的 `SKILL.md`,
  应用只摆出来给你看一眼,**不需要你填任何东西,也不会替你改文件**。
- 分享前会按团队标准检查一遍:`name` 要有、要和文件夹名一模一样、只能用英文小写
  字母/数字/短横线;`description` 要有且不超过 1024 个字符。不合格的技能**照常列
  出来**,只是「分享」点不动,旁边直说是哪一条不合格,并给一个「打开文件夹」让你去改。
- 名字撞车(技能库里已经有同名技能、但不是你分享的)也一样直说,请你在本地改个
  文件夹名。没有"覆盖别人的技能"这条路。
- **这条检查对"分享改动"同样生效**:如果你手上某个技能的 `name` 与文件夹名对不上
  (比如从技能广场装来的一部分技能,或早年用中文名分享过的),它的「分享改动」按钮
  从这一版起会点不动,旁边会写明是哪一条不合格。这是有意为之——不合格的技能推上去,
  别的遵循同一标准的工具不保证认得出它。
- 单独的「分享」页去掉了,新建技能与分享都在「我的技能」上完成——你的技能本来就
  都列在那儿。

**「我的技能」重新设计成三栏,按公司技能库分**

- **安装自技能库**(装的是同事分享的)/ **已分享到技能库**(技能库里记的分享者是你)/
  **可分享到技能库**(自己写的草稿,或者从商店广场、GitHub 等别处装来、公司技能库里
  还没有的技能)。哪一栏没有内容就不显示,不用看空标题。
- 每一行**最多摆一颗按钮,而且只在有事要做的时候才摆**:库里出了新版本就是「更新」;
  你在本地改了、库里还没变就是「贡献更改」;两边都变了,弹窗让你选「库里有新版…」
  怎么处理;技能库里正在审核你提交的这份改动,这一行会写着「审核中」,有链接的话
  还给一个「在技能库里查看」,没有按钮可点、等结果就好;
  同一个技能在这台电脑上有好几份内容不一样的文件时,任何一栏都会先弹「选择保留
  哪一份」。什么都不用做的行就干干净净、不摆多余的状态字。
- **贡献更改一律走「提交审核」**——哪怕你平时能把改动直接分享进这个技能库,
  贡献自己改过的内容这条路也统一先经过审核,不会直接覆盖库里的内容。
- 技能详情页「在哪」这一块补全了:这台电脑上的哪个文件夹、各个工具里的启用状态
  (还是那一排勾)、技能库里的信息——库名、作者、和你本地这一份的版本关系,
  正在审核的话还给一个「在技能库里查看」的链接。
- 顶上加了搜索框,跟商店那个用法一样。
- 各个工具的勾选控件统一了:获取技能时的选工具面板、技能详情页「各个工具里」、
  把技能装进某个项目文件夹时的确认条,现在用的是同一套控件,勾选逻辑与显示顺序
  完全一致。
- 页面按「通用」(这台电脑上的技能)和「项目里」两个页签分开;装进项目里的技能
  现在也能事后改选让哪些工具用它,不必卸载重装。
- 来源展示统一了:不管是本应用装的还是别的工具装的,只要看得出它来自哪个技能库,
  都显示同一种格式(比如 `团队/技能库名`)。

**这一版没有覆盖到的**:项目文件夹里的技能仍然不参与上面的三栏分类,安装、
更新、移除、无法自动检查更新、不能分享回推这些规矩都还是上一版的老样子——
这一版只给它们加了「事后改选工具」和独立页签。

## 0.5.0 · 2026-08-22 —— 技能可以只装进某个项目文件夹

此前技能只有一种装法:装在这台电脑上,所有 AI 工具、所有项目都看得到。
这一版多了第二种:**把技能装进某个具体的文件夹,只在那里生效**。

适合"这个技能只跟这个项目有关"的情况——比如某个项目专用的代码规范、
某个客户的文档模板,不必让它出现在你所有的工作里。

- **入口在「获取」按钮旁边**。还没装的时候是个小箭头,点开选「装到项目…」,
  挑一个文件夹即可;装过之后它会变成写着「装到项目…」的按钮,一眼看得见。
  菜单里会列出最近用过的文件夹,下次点一下就到。
  主按钮「获取」的行为**一点没变**,只装到这台电脑上——只用这一种装法的话,
  什么都不用改。
- **「我的技能」多了「装在项目里的」分区**,按文件夹分组列出。
  每个技能可以单独更新或移除;整个文件夹可以「从列表移除」,那只是不再显示它,
  **文件夹和里面的技能一个字节都不会动**。
  文件夹被删掉或移走时,这里会如实说明。
- **与命令行工具完全互通**。装进项目的技能,用的是和 `npx skills` 完全相同的
  存放方式与记录格式,所以两边能互相认出对方装的技能:你在应用里装的,
  同事用命令行也看得到、也能更新,反过来也一样。

**装到项目的操作更稳当了:**

- 选完文件夹**不会立刻开装**,而是先摆出来给你看一眼:装到哪个文件夹、完整路径、
  会在哪些工具里能用,点「装到这里」才真的装。从「最近的项目」里点也是同一套流程,
  只是省掉了挑文件夹那一步。
- 这个文件夹里已经有这个技能时,**在你点之前就告诉你**,不会让你白等一次下载;
  想重装一遍(比如那个技能被别的工具弄坏了)也随时可以,按钮换成「覆盖重装」。
  你自己改过的内容仍然会先问过你再动。
- 确认时可以直接「换个文件夹」,不必退出去重来。
- 技能详情里新增「已装到」,列出它装在这台电脑上的哪些位置(全局、各个项目文件夹),
  点一下就能跳到「我的技能」里对应的地方。
- **装完之后那一屏也留着入口**,想接着装到别的文件夹不必先关掉详情再打开。
- 刚装完显示的「已启用到 Claude Code、Trae」是**这一次安装的结果**,关掉详情
  再打开就恢复成简单的「已启用」——和你很久以前装的技能长得一样,不再需要重启应用。

**另外:升级之后终于知道改了什么。**

- 装上新版第一次打开时,商店页顶部会出现一张卡片,说清这一版改了什么。
  看完点右上角关掉即可,**不点它就一直在**——升级完随手退出的话,下次打开还能看到。
- 一口气跨了好几个版本时,漏看的那几版收在卡片下方,点开就能展。
- 平时不打扰。想回头翻旧版本的说明,去「设置 → 版本历史」,每一版都带发布日期。

几条这一版有意收窄的范围,如实说明:

- **自动更新只管装在这台电脑上的技能**。项目里的技能要更新,去「我的技能」里点。
- **项目里改过的技能不能分享回技能库**。要分享,先在这台电脑上装一份再改。
- **来源找不回去的技能不给「更新」按钮**(比如别人手放进项目的)。
  应用不知道该去哪儿取新版本,所以不摆这个按钮,而不是摆一个点了必然失败的。

## 0.4.0 · 2026-08-20 —— 技能广场:搜遍全网技能,一键装进来

这一版带来一个全新的地方:**技能广场**。此前只能装公司技能库里的技能,
现在商店的库切换器里多了固定的一档「技能广场」,可以搜到并安装全网公开分享的
AI 技能(数据来自 skills.sh)。

- **打开就有得看**。切到「技能广场」不用先想搜什么,首屏直接列出全网热门技能,
  卡片样式与公司技能库一致。
- **搜索是输入完按回车**。打字过程中不会发请求,所以输入很跟手;想搜的时候按一下
  回车即可。同一个词连按回车不会重复发请求,换个词则立刻以新的为准——网络慢的时候
  也不会卡着等上一次的结果。清空搜索框就回到热门榜。
- **列表滚到底自动接上**,不用翻页。
- **装完就是普通技能**:出现在「我的技能」里,会自动检查更新,用法与公司技能库
  装的技能完全一样。卡片上还有「在浏览器中查看」,可跳到 skills.sh 的技能页。
- **读详情和安装都快了很多**。此前为了展示一个技能的说明文档,要把整个技能库
  下载下来(动辄几 MB、几十秒);现在改成只取这个技能的内容,常见情况下一两秒
  就能看到。安装同理,纯文本技能走快路径,带脚本或图片的仍走完整下载以保证装得完整。
- **macOS 上的钥匙串询问从三次减到一次**。此前每次启动应用要连点三次「允许」,
  现在只问一次(点「始终允许」之后就不再问)。
  注意:已经登录过的话,需要**退出登录再登录一次**才会生效。

公司技能库的一切照旧,不受影响。技能广场需要能访问外网(skills.sh 与 github.com);
公司网络不通或没连 VPN 时,这一档会提示连不上,**不影响公司技能库的使用**。

## 0.3.13 · 2026-08-11 —— 窗口终于能用鼠标拖动了

- **修复窗口拖不动**。此前按住窗口顶部空白处怎么拖都没反应,两个平台都一样。
  0.3.12 里那次修复没解决问题——真正的原因不在界面,而是应用少授了一项系统权限,
  导致「开始拖动窗口」这个请求被挡掉,界面上看不出任何异常。现在按住左上角空白处
  (侧边栏顶部)或工具栏的空白处即可拖动整个窗口。

## 0.3.12 · 2026-08-11 —— 更新不再打断你,窗口和托盘也听话了

- **Windows:应用更新不会再自己关掉打断你**。此前新版本一出现,应用就会在你用着的时候
  突然退出去装更新。现在改成后台悄悄下载好,左下角出现「新版本已就绪」的提示,
  **你点了「立即重启」才开始安装**。(Windows 上装更新必须先关掉应用,所以点完
  会先看到安装程序跑一下,装完自动打开新版本。macOS 一直就是这个行为,不受影响。)
- ~~**窗口拖不动的问题修好了**~~ —— **这条没做到,以 0.3.13 为准**。当时改的地方
  本来就是好的,真正的原因是少授了一项系统权限,拖动请求被挡掉了。
- **macOS:关掉窗口后,点程序坞里的图标能重新打开窗口了**。此前点了没有任何反应,
  看起来像卡死——其实应用一直在后台好好运行着。
- **托盘/菜单栏图标改成左键开窗口、右键出菜单**,与其他应用一致。此前左键点也是弹菜单。

## 0.3.11 · 2026-08-07 —— 修复 Windows 上登录不了

- **修复 Windows 登录失败**。在 Windows 上点完授权回到应用「没有反应」、始终登录不上。
  原因是登录凭证一次性写入 Windows 凭据管理器时超出了它的单条大小限制,现在改为
  分片保存。macOS 不受影响,升级后也不需要重新登录。
- 登录过程现在会记录诊断日志(`%USERPROFILE%\.skillsync\logs\`),万一还有问题,
  把日志发出来就能定位。日志不含任何令牌或授权码。
- 首次启动向导里,登录失败现在会把原因显示出来——此前是静默失败,什么都不提示。

## 0.3.10 · 2026-08-07 —— 首个 Windows 版本

- **首次提供 Windows 安装包**(`SkillSync_0.3.10_x64-setup.exe`,64 位)。
  按当前用户安装,不需要管理员权限、不弹 UAC。
- Windows 首次运行会被 SmartScreen 提示「未知发布者」(安装包尚未代码签名),
  点「更多信息」→「仍要运行」即可。
- 自动更新公告牌同时覆盖 macOS 与 Windows。
- macOS 侧无功能变化。

## 0.3.9 · 2026-08-07 —— 修复作者栏不显示

- 修复技能卡片与详情里**作者/贡献者栏始终不出现**的问题。根因是旧版本建立的
  索引缓存里没有这个字段,而新版本一直命中那份旧缓存。升级后自动重建。

## 0.3.8 · 2026-08-07 —— 作者与贡献者展示

- 技能卡片和详情页显示**作者与贡献者**,数据随技能库索引一起下来,不额外联网。
- 分享技能时自动维护归因信息:新分享记你为作者,更新他人技能则追加为贡献者。

## 0.3.7 · 2026-08-07 —— 新装默认每 5 分钟检查技能

- 全新安装的默认技能检查频率改为「每 5 分钟」(此前是每 4 小时)。
  已经装过的机器保留你自己的设置,不会被改回去。

## 0.3.6 · 2026-08-07 —— 新增「每 5 分钟」检查档位

- 技能自动检查新增「每 5 分钟」档位。
- 注意:本地配置格式随之升级,**装过 0.3.6 之后不能回装更早的版本**
  (老版本读到新格式会进只读模式并提示升级,这是有意的保护)。

## 0.3.5 / 0.3.4 · 2026-08-07 —— 自更新链路验证

- 为验证 0.3.3 的自更新与重启修复而连发,无独立功能改动。
  (改更新机制天然要两跳:一跳把机制送到机器上,一跳才能让它自己跑起来。)

## 0.3.3 · 2026-08-07 —— 更新提示更及时、重启后窗口不再沉底

- 应用更新检查改为**每分钟一次**,与技能检查频率彻底解耦。此前技能设成「手动」时,
  应用更新就只在启动时查一次,等于「什么都没发生」。
- 修复 macOS 上点「立即重启」后**窗口沉到所有应用后面**、要点程序坞图标才出来的问题。

## 0.3.2 · 2026-08-07 —— 自更新的独立节拍

- 从设置页手动安装更新后,左下角也会出现重启提示(此前只有自动轮次才提示)。

## 0.3.1 · 2026-08-06 —— 验证新自更新链路

- 专为验证 0.3.0 的静默自更新链路而发,无独立功能改动。

## 0.3.0 · 2026-08-06 —— 多来源、新建技能、静默自更新

- 支持**多个技能库来源**(公司 Gitea 之外可添加自定义源与 GitHub)。
- 支持**新建技能**,以及把已装的技能登记到本应用里管理(这半个功能已于 0.6.0 撤销:
  归属现在直接看技能库里记的分享者,不需要手动登记)。
- 应用更新改为**静默装好 + 左下角提示重启**,不再打断正在做的事。

## 0.2.2 / 0.2.1 · 2026-08-05 —— 自更新修复

- **0.2.1** 修复了 `0.1.0`/`0.2.0` 收不到自动更新的缺陷(更新源使用 http 时
  被客户端拒绝,一个请求都发不出去)。装的是那两版需要手动装一次新包。
- **0.2.0 已下架**。

## 0.2.0 · 2026-08-05 —— 冲突协作与商店标签

- 更新技能时若本地有改动,提供「保留本地」「用远端覆盖」「保留并分享」三种选择。
- 「我的技能」分区展示;商店支持标签筛选与精选清单。

## 0.1.0 · 2026-08-04 —— 首个内部测试版本

- 技能商店浏览、一键获取、自动更新、分享到技能库。
- 注意:此版本**收不到自动更新**(更新源缺陷),需手动装新包。
